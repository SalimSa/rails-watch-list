<div class= "container mt-5">
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: Movie.all%>
  <%= f.submit "Submit bookmark", class: "btn btn-primary" %>
<% end %>
 <div>

<br>

<div class="fa-solid fa"> <%= link_to "Go back to lists", lists_path %>
</div>
